import { Fragment } from "react";
import React from "react";
import Form from "../containers/Form";

const Home = () => {
  return (
    <Fragment>
      <Form />
    </Fragment>
  );
};

export default Home;
